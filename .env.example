# Same Supabase project as the Acedex app. The anon key is safe to expose in a
# client bundle — Row-Level Security (see migration 023_waitlist.sql) is what
# actually protects the data: the public can INSERT into waitlist but cannot
# read it.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
